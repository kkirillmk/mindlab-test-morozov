# API

API для управления пользователями и ролями с JWT аутентификацией на Laravel 11.

## 📋 Описание

API предоставляет функциональность для:
- JWT аутентификации (access token + refresh token)
- Управления пользователями (CRUD операции)
- Управления ролями (RBAC)
- Смены паролей (своих и администратором)
- Активации/деактивации пользователей (увольнение)

## 🛠 Технологический стек

- **PHP 8.3**
- **Laravel 11**
- **MySQL 8.0**
- **Redis 7.2**
- **JWT Auth** (php-open-source-saver/jwt-auth)
- **Docker & Docker Compose**
- **Nginx**
- **Scribe** (автоматическая генерация API документации)


## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd mindlab-test-morozov
```

### 2. Настройка окружения

Создайте файл `.env.docker` в корне проекта:

```bash
cp .env.docker.example .env.docker
```


### 3. Запуск проекта

```bash
# Запуск всех сервисов
docker compose up -d

# Генерация ключа приложения
docker compose exec app php artisan key:generate

# Генерация JWT секрета
docker compose exec app php artisan jwt:secret

# Выполнение миграций
docker compose exec app php artisan migrate

# Заполнение базы данных тестовыми данными
docker compose exec app php artisan db:seed
```


## 🔐 Учетные данные по умолчанию

После выполнения сидеров создается администратор:

- **Email**: `admin@example.com`
- **Пароль**: `password`
- **Роль**: `admin`

## 📚 API Документация

Автоматически сгенерированная документация доступна по адресу:
- **HTML**: /docs
- **Postman Collection**: /docs.postman
- **OpenAPI Spec**: /docs.openapi

Для обновления документации:

```bash
docker compose exec app php artisan scribe:generate
```
